/*   
Create a function that will search a folder for a requested file. 
The function should return true if the file is present, and false if not
The file name will be represented by the object properties/keys

e.g.
const searchFolder = {
    secrets: 'contents...',
    notSecrets:'not secret stuff...'
}
containsFile(searchFolder, secrets) // returns true
*/

const containsFile = (folder, requestedFile) => {};
